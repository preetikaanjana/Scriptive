name: Greetings

on: [pull_request, issues]

jobs:
    greeting:
        runs-on: ubuntu-latest
        permissions:
            issues: write
            pull-requests: write
        steps:
            - uses: actions/first-interaction@v1
              with:
                  repo-token: ${{ secrets.GITHUB_TOKEN }}
                  issue-message: 'Welcome to Handwritten! 🖋️ Thank you for opening your first issue. Our team will look into it soon!'
                  pr-message: 'Thank you for your first Pull Request to Handwritten! 🚀 We appreciate your contribution. A maintainer will review your changes shortly.'
